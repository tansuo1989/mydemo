import { MytestDirective } from './mytest.directive';

describe('MytestDirective', () => {
  it('should create an instance', () => {
    const directive = new MytestDirective();
    expect(directive).toBeTruthy();
  });
});
